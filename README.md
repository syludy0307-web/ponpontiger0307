# ワールドミステリーch ／「夢の中で死ぬと現実でも死ぬ」説

動画収録用の HTML ビジュアル。古書を捲っていく形式で台本を進行します。
**16:9（1920×1080）固定**、そのまま画面収録すれば映像素材になります。

## 使い方

`index.html` をブラウザで開いて、画面をクリック → 本が開きます。
（そのままダブルクリックで開いても、ローカルサーバー経由でも動作します）

```bash
# サーバー経由で開く場合
npx http-server -p 8080 .   # → http://localhost:8080/
```

推奨は **Chrome / Edge** の全画面（`F`キー）。1920×1080 のディスプレイなら等倍で表示されます。

### 操作

| キー | 動作 |
|---|---|
| `Space` `→` | 次のページへ（3Dページめくり＋雫のSE） |
| `←` | 前のページへ |
| `P` | 自動再生 / 一時停止 |
| `[` `]` | 再生速度 ×0.5 〜 ×2.0 |
| `I` | 目次（章ジャンプ） |
| `W` | ワイプ枠の切替：透明 → 黒 → グリーン → マゼンタ → カメラ |
| `C` | UI を全部隠す（**収録時はこれ**） |
| `M` | SE ミュート |
| `F` | 全画面 |
| `Home` / `End` | 最初 / 最後へ |

本の左半分クリックで戻る、右半分クリックで進む、でも操作できます。
マウスを 3 秒動かさないと UI は自動で消えます。

### 収録の手順

1. 全画面（`F`）→ `C` で UI を消す
2. `P` で自動再生を開始（台本の尺どおりに自動でめくれます）
3. 画面収録
4. ナレーションと BGM は編集で乗せる（このページは SE しか鳴りません）

自動再生は台本のタイムコードに合わせてあり、**全 41 見開き / 約 14分51秒**。
ナレーションと合わない場合は `[` `]` で速度を調整するか、`P` で止めて手動でめくってください。

### ワイプ枠（右下）

枠の中は**本当に切り抜かれています**（枠のリングだけを描画）。

- **透明** … 背景がそのまま透ける。編集ソフトで上にカメラ映像を重ねる用
- **グリーン / マゼンタ** … クロマキー用のベタ塗り（この時は反射エフェクトが自動で消えます）
- **黒** … 単色で塞ぐ
- **カメラ** … ブラウザのカメラをその場でプレビュー（確認用）

枠の下の「案内人」は**その場で書き換えられます**（クリックして入力）。

## 中身

- **背景** … 廃墟の実写＋ピラネージの銅版画を重ね、常時ゆっくりドリフト
- **埃** … canvas パーティクル 250 個。手前の大粒はボケ・奥は細かく、常時漂う
- **右上ロゴ** … 「ワールドミステリーch」がネオン管のように不規則に明滅＋たまにグリッチ
- **ページめくり** … `rotateY` の実 3D。表裏で別ページを持ち、影・落ち影・小口の厚みが進行に合わせて変化
- **SE** … アップロードされた雫の音（`assets/se/page-drop.mp3`）をめくるたびに再生
- **本文** … 版面からはみ出さないよう、級数 → 行間 → 段間の順に自動で詰めます（22.5〜25px に収まる）

**OP（0:00〜0:40）は台本どおり除外**しています。冒頭は扉ページのみ、本編は第1章「誰も確認できない奇妙な説」から始まります。

## 図版

`assets/img/` の 14 点はすべて **パブリックドメイン / CC0**（Wikimedia Commons）。
そのまま YouTube に載せて問題ありません。奥付ページにも一覧を出しています。

| | 図版 | 作者 |
|---|---|---|
| 第1章 | The Fall of Lucifer | Gustave Doré, 1866 |
| 第2章 | Gray's Anatomy 図728（脳） | H. V. Carter, 1858 |
| 第3章 | The Soul Hovering over the Body | Blake / Schiavonetti, 1813 |
| 第4章 | アケローン川を渡る亡者たち | Gustave Doré, 1861 |
| 第5章 | The Deluge | Gustave Doré, 1866 |
| 第6章 | **The Nightmare（夢魔）** | Henry Fuseli, 1781 |
| 第7章 | **マニラ近郊の街路（実写）** | 米国議会図書館, 1899 |
| 第8章 | Gray's Anatomy 図497（心臓） | H. V. Carter, 1858 |
| 第9章 | 理性の眠りは怪物を生む | Francisco Goya, 1797-98 |
| 第10章 | Jacob's Ladder | William Blake, 1805 |
| 第11章 | Carceri d'invenzione III | G. B. Piranesi, c.1749 |
| 第12章 | **Ophelia** | J. E. Millais, 1851-52 |
| 背景 | Peressaare school hall（廃校）/ The Drawbridge | Hannu / Piranesi |

図解（睡眠段階・自律神経・刺激変換・各地の呼称・心電図・研究メモ・確証バイアス・因果の順序・最終検証）は
すべて SVG で自作。**数値をでっち上げないよう、台本にない数字はグラフ化していません**
（2025年の研究は数値グラフではなく「研究ノート」として提示しています）。

## 手を入れる

`index.html` 一枚に全部入っています。

- **台本** … `CHAPTERS` 配列。`lines` に文を並べるだけ。`{c:"q", t:"…"}` で赤字強調
- **見開きの左ページ** … `P()` 図版 / `Q()` 引用 / `F()` 図解 / `X()` 部分拡大
- **尺** … 各章の `sec`。章内は文字数比で自動配分されます
- **図版の差し替え** … `IMG` のパスを変えるだけ（`fit:"contain"` で全体表示）
- **レイアウト** … CSS 冒頭の `:root` 変数（本の位置・サイズ、ワイプの位置・サイズ）
